

<ul class="list">
  <slot />
</ul>

<style is:inline>
  .list {
    display: flex;
    gap: 1rem;
    flex-direction: column;

    @media (width >= 48rem) {
      flex-direction: row;
    }
  }
  .list-item {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 2rem;
    padding-block: .5rem;
  }

</style>